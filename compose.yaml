services:
    web:
        image: node:20-alpine
        working_dir: /app
        command: npm start
        ports:
            - '8080:8080'
        build: .

        volumes:
            - .:/app
            - /app/node_modules
        environment:
            - CHOKIDAR_USEPOLLING=true
